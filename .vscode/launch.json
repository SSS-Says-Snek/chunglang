{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "codelldb",
            "request": "launch",
            "name": "Debug Compiler and Parse `test.chung`",
            "preLaunchTask": "Build (Debug) and Compile Project",
            "expressions": "native",
            "program": "${workspaceFolder}/build/chung",
            "args": ["parse", "test.chung"]
        }
    ]
}
